/**
 * App Light Theme
 */
import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
   palette: {
      primary: {
         main: '#26348F'
      },
      secondary: {
         main: '#9C27B0'
      }
   }
});

export default theme;